{# Modèle de base #}
{% extends "base.twig" %}

{# Contenu de la page demandé #}
{% block content %}
	{# Description de la page #}
	<p>{{ console_phrases["console_description"] }}</p>

	{# Contrôleur principal #}
	<section id="controller">
		{# Titre de la section #}
		<h2>{{ console_phrases["console_controller_title"] }}</h2>

		{# Champ de saisie #}
		<div>
			<input type="text" name="command" placeholder="{{ console_phrases["console_controller_placeholder"] }}" required />

			<button type="button" name="execute" value="bash">
				<i class="bi bi-send-fill"></i>
			</button>
		</div>

		{# Historique des commandes #}
		<ul>
			{# Première commande #}
			<li>command_name value</li>

			{# Deuxième commande #}
			<li>command_name value</li>

			{# Troisième commande #}
			<li>command_name value</li>
		</ul>
	</section>

	{# Terminal de la console #}
	<section id="terminal">
		{# Titre de la section #}
		<h2>{{ console_phrases["console_terminal_title"] }}</h2>

		{# Liste des événements #}
		<ul>
			{# Première ligne #}
			<li>Lorem ipsum</li>

			{# Deuxième ligne #}
			<li>Lorem ipsum</li>

			{# Troisième ligne #}
			<li>Lorem ipsum</li>
		</ul>
	</section>
{% endblock %}