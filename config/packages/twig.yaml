twig:
    default_path: "%kernel.project_dir%/templates"
    globals:
        global_analytics: "%env(ANALYTICS_TAG)%"
        global_recaptcha: "%env(RECAPTCHA_PUBLIC_KEY)%"

when@test:
    twig:
        strict_variables: true