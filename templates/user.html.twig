{# Modèle de base #}
{% extends "base.html.twig" %}

{# Injection de code JavaScript #}
{% block javascript %}
	<script>
		// Libellé demandant la suppression ou non du compte utilisateur.
		window.edit_remove = "{{ t("user.edit_remove")|trans }}";
	</script>
{% endblock %}

{# Contenu de la page demandé #}
{% block content %}
	{# Description de la page #}
	<p>{{ t("user.description")|trans }}</p>

	{# Informations du compte #}
	<section id="account">
		{# Titre de la section #}
		<h2>{{ t("user.account")|trans }}</h2>

		{# Champs du formulaire #}
		<form novalidate>
			{# Nom d'utilisateur #}
			<div>
				<label for="username">{{ t("form.register.username")|trans }}<span>*</span></label>
				<input type="text" autocomplete="username" spellcheck="false" id="username" name="username" placeholder="johndoe" minlength="10" maxlength="50" required />
			</div>

			{# Mot de passe #}
			<div>
				<label for="password">{{ t("form.register.password")|trans }}<span>*</span></label>
				<input type="password" autocomplete="current-password" spellcheck="false" id="password" name="password" placeholder="?JohnDoe2022!" minlength="10" maxlength="60" required />
			</div>

			{# Suppression/mise à jour #}
			<div>
				<input type="hidden" name="token-remove" value="{{ csrf_token("app_user_remove") }}">
				<input type="submit" data-action="remove" data-route="{{ path("app_user_remove") }}" value="{{ t("global.remove")|trans }}" />

				<input type="hidden" name="token-update" value="{{ csrf_token("app_user_update") }}">
				<input type="submit" data-action="update" data-route="{{ path("app_user_update") }}" value="{{ t("global.update")|trans }}" />
			</div>
		</form>
	</section>

	<section id="actions">
		{# Titre de la section #}
		<h2>{{ t("user.actions")|trans }}</h2>

		{# Bouton de déconnexion #}
		<input type="hidden" name="token-logout" value="{{ csrf_token("user_logout") }}">
		<input type="submit" data-action="logout" data-route="{{ path("app_user_logout") }}" value="{{ t("user.disconnect")|trans }}" />

		{# Bouton de reconnexion #}
		<input type="hidden" name="token-login" value="{{ csrf_token("user_login") }}">
		<input type="submit" data-action="login" data-route="{{ path("app_user_login") }}" value="{{ t("user.reconnect")|trans }}" />
	</section>

	{# Ajout d'un serveur #}
	<section id="register" data-route="{{ path("app_user_register") }}" data-redirect="{{ path("app_dashboard_page") }}">
		{% include("form_register.html.twig") %}
	</section>
{% endblock %}